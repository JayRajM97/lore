// WEB: home-screen widgets don't exist here — no-ops.
import { Episode } from "./types";

export function recordWidgetPlay(_episode: Episode) {}
export function seedWidgetEpisodes(_episodes: Episode[]) {}
