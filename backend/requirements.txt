fastapi
uvicorn[standard]
pydantic

# TTS
kokoro>=0.9.2
soundfile
numpy

# Google / Gmail
google-auth
google-auth-oauthlib
google-api-python-client

# Firebase Admin (Firestore + Storage from server)
firebase-admin

# Audio encoding
# ffmpeg = system binary (brew install ffmpeg)
